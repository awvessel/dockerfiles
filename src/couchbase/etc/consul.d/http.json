{
    "service": {
        "name": "couchbase:admin",
        "port": 8091,
        "check": {
            "name": "Couchbase HTTP Check",
            "notes": "Use curl to check the web service every 10 seconds",
            "script": "curl localhost:8091 >/dev/null 2>&1",
            "interval": "10s"
        }
    }
}
