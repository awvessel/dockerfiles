{
    "check": {
        "name": "Raft Port",
        "notes": "Connect to the port internally every 10 seconds",
        "script": "nc -z localhost 8090 >/dev/null 2>&1",
        "interval": "10s"
    }
}
