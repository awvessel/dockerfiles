{
    "check": {
        "name": "InfluxDB Admin",
        "notes": "Use curl to check the web service every 10 seconds",
        "script": "curl localhost:8083 >/dev/null 2>&1",
        "interval": "10s"
    }
}
